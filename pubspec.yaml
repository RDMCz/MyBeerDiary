name: my_beer_diary
description: "Aplikace Můj pivní deníček"
publish_to: 'none'
version: 0.2.0

environment:
  sdk: ^3.12.2

dependencies:
  escape_parent_padding: ^1.0.4
  file_picker: ^13.1.0
  fl_chart: ^1.2.0
  flutter:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4
  flutter_svg: ^2.3.0
  path: ^1.9.1
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  sqflite: ^2.4.3
  sqflite_common_ffi: ^2.4.2
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - asset/icon/

flutter_icons:
  android: true
  image_path: "asset/icon/appicon.png"
